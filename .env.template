
# Backend API base URL (optional).
# Defaults to https://relax-map-api.onrender.com
API_URL=

# Public base URL for the Next.js app (optional).
# Leave empty to use same-origin requests (recommended for local dev).
NEXT_PUBLIC_API_URL=
